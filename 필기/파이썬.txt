파이썬은 main함수가 없다.
컴파일 과정도 없다.
코딩한 다음 바로 실행됨
파이썬을 설치하면 인터프리터도 함께 설치됨.
인터프리터는 파이썬을 실행하는 기술임.

자바와 다른점
.class를 실행하는 거싱 아니라 코드를 즉시 실행함.

함수 + 모듈 = 표준 라이브러리

import random


변수 동적으로 할당
변수의 유형을미리 선언할 필요가 없다.